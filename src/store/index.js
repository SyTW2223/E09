import { createStore } from 'vuex'
import { userModel} from './modules/userModel'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user: userModel,
  }
})
